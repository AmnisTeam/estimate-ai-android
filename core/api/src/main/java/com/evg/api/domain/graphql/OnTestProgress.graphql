subscription OnTestProgress($topicId: ID!) {
    onTestProgressResponse(topicId: $topicId) {
        code
        loadingTest {
            id
            progress
            queue
            createdAt
        }
    }
}
