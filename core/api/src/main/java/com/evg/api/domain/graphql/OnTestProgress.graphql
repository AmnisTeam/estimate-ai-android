subscription OnTestProgress($topicId: ID!) {
    onTestProgress(topicId: $topicId) {
        code
        loadingTest {
            id
            progress
            queue
            createdAt
        }
    }
}
