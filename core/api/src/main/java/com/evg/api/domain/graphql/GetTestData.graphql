query GetTestDataResponse($id: Int!) {
    getTestDataResponse(id: $id) {
        code
        testData {
            ... on EssayTest {
                id
                essay
            }
        }
    }
}
