query GetTests($page: Int!) {
    getTestsResponse(page: $page) {
        code
        count
        pages
        next
        prev
        tests {
            ... on ReadyTest {
                id
                title
                type
                status
                level
                createdAt
            }
            ... on LoadingTest {
                id
                progress
                queue
                createdAt
            }
            ... on ErrorTest {
                id
            }
        }
    }
}
