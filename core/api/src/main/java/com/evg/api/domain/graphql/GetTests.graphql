query GetTests($page: Int!) {
    getTestsResponse(page: $page) {
        code
        count
        pages
        next
        prev
        tests {
            ...TestResultFragment
        }
    }
}